---
"@getpixel/ui": patch
"@getpixel/server": patch
---

Document `<PixelStateRoot>` in the install path (README + pixel skill) so fresh installs include the time-travel remount boundary instead of skipping it.

