---
"@getpixel/ui": minor
---

Add `@getpixel/ui/next` (`withPixel`) and `@getpixel/ui/vite` (`pixelReactAlias`) bundler helpers so Next.js and Vite apps need only one or two integration calls instead of hand-rolled webpack/Vite alias config.
