{
  "name": "pixel",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @pixel/ui && npm run build -w @pixel/server",
    "server": "npm run dev -w @pixel/server",
    "example": "npm run dev -w @pixel/example",
    "dev": "npm-run-all --parallel server example"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
