{
  "name": "pixel",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @getpixel/ui && npm run build -w @getpixel/server",
    "server": "npm run dev -w @getpixel/server",
    "example": "npm run dev -w @getpixel/example",
    "dev": "npm-run-all --parallel server example"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
